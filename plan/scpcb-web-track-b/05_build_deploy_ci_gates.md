# Plan 05 — Build/Deploy + CI Gates (Reproducible, Enforced Contracts)

Created: 2026-01-29
Last updated: 2026-01-29 16:56 EST

Goal: build outputs are reproducible and enforce Track B constraints automatically.

## A) Build Outputs

- [x] Ensure `dist/` is self-contained. (2026-01-29)
  - [x] `scpcb.wasm` (copied by `web/build.ts`). (2026-01-29)
  - [x] `scpcb_manifest.json` (generated by `web/build.ts`). (2026-01-29)
  - [x] `assets/**` (no source formats; conversion happens during build). (2026-01-29)
- [x] Add size reporting:
  - [x] total `dist/` size (2026-01-29: `Tools/dist_report.ts`)
  - [x] top 20 largest assets (2026-01-29: `deno task dist:report`, wired into `test:web:build`)

## B) Enforced Contracts

- [x] Build-time purge of `.b3d/.x/.rmesh` copied from `web/public`. (2026-01-29)
- [x] Build-time validation gate (fails if leaked). (2026-01-29)
- [x] Add manifest gate: ensure no missing files referenced by manifest groups. (2026-01-29: `Tools/validate_manifest_files.ts` wired into `deno task test:web:build`)
- [x] CMDB wasm export contract checker (Track B). (2026-01-29: `Tools/validate_cmdbuf_wasm.ts` + `Tools/tests/cmdbuf_wasm_check.test.ts`)
- [x] Build-time gate: fail if `dist/scpcb.wasm` is not Track B (`__CmdBuf*` exports missing). (2026-01-29: `web/build.ts`)

## C) CI Tasks

- [ ] Standardize CI pipeline:
  - [x] `deno task test:swift` (exists in `deno.json`). (2026-01-29)
  - [x] `deno task test:deno` (exists in `deno.json`). (2026-01-29)
  - [x] `deno task test:web:build` (exists in `deno.json`). (2026-01-29)
  - [x] `deno task test:wasm` (exists in `deno.json`). (2026-01-29)
- [ ] Add “audit gate” to detect new source-format references in BB code.
- [x] Add progress reporting for long compiles (NDJSON) so wrappers can show a progress bar. (2026-01-29: `Tools/wasm-cli/main.swift` `--progress ndjson`, includes per-function codegen progress; used by `Tools/compile_scpcb_main.ts`)

Acceptance Criteria:
- [ ] CI fails immediately if `.b3d/.x/.rmesh` would be shipped.
- [x] CI produces a stable artifact with the same manifest hash given the same inputs. (2026-01-30: sort `files` and group lists in `web/build.ts`)
