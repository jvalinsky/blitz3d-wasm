---
title: "TODOs: SCPCB Browser Parity (Next Actions)"
status: "active"
created_at: "2026-01-21T23:20:00Z"
---

# Scope

Concrete, executable TODOs derived from:

- `docs/plans/2026-01-21-scpcb-browser-parity-overview.md`
- `docs/plans/2026-01-21-scpcb-browser-100-percent-plan.md`
- `docs/plans/2026-01-21-bbdbg-live-events-integration.md`
- `docs/plans/2026-01-21-bb-wasm-debugger-visualizer.md`
- `docs/plans/2026-01-21-blitz3d-wasm-wasm-debug-info-implementation-checklist.md`

# Current baseline (done)

- [x] Span-carrying AST enums are integrated and build clean.
  - Repair notes:
    `blitz3d-wasm/plan/2026-01-21-span-carrying-ast-migration-repair.md`

# Phase 0 — Baseline + scoreboard (do first)

- [ ] Add a single “scoreboard” script + output format:
  - Inputs: `blitz3d-wasm/compile_errors_*.json` and/or a fresh `scpcb/**/*.bb`
    compile run
  - Output: % compile success, % wasm-validate success, top error categories,
    top failing files
  - Gate: report is regenerated in < 60s and committed as artifact text/JSON
- [ ] Freeze the vertical-slice demo target:
  - Demo: `blitz3d-wasm/Examples/scpcb_facility_walk/`
  - Gate: no more “switch meshes” during bug hunts unless the mesh is proven
    invalid/unusable

# Phase 1 — Compiler correctness (WASM validity is the gate)

- [ ] Add (or tighten) a “validator-first” gate for the slice:
  - `wasm-validate` must succeed for the slice `.bb` output
  - If invalid: compiler must emit a source span and a stable error category
- [x] Ensure “Pow” is not a weak fallback:
  - Added `Pow` import (env, f32→f32), codegen calls it; runtime exposes `Pow`
    via `Math.pow`
  - Fallback keeps stack balanced if import missing
- [ ] Audit remaining SCPCB blockers from the latest compile report:
  - Update the failing-file list and group by root cause
    (parse/type/stack/import)
  - Gate: each category gets at least one minimal regression `.bb` snippet

# Phase 2 — Authoritative import registry (stop signature drift)

- [ ] Implement “required imports scan” for `scpcb/**/*.bb`:
  - Output: a deterministic list of `(module, name, signature, callSites[])`
  - Gate: diffable output; changes are reviewable PR-to-PR
- [ ] Add a “provided vs required” check against the runtime shim:
  - Gate: slice requires ⊆ runtime provides (no missing, no type mismatch)

# Phase 3 — Debugging v1 (bbdbg stepping)

- [ ] Add compiler `--debug` flag:
  - Emits bbdbg imports and injects `bbdbg.stmt` at statement boundaries
  - Gate: instrumentation is behind flag; non-debug builds are byte-for-byte
    unchanged (or documented deltas)
- [ ] Emit `program.bbdbg.json` mapping:
  - Minimum: `stmtId -> { file, line, col }` from `SourceSpan`
  - Optional: function table (`funcId`, name, span)
  - Gate: browser visualizer can highlight the correct BB line for each emitted
    `stmtId`
- [ ] Make stepping semantics correct in the visualizer:
  - Loop stepping, function call/return nesting, break/continue/return handling
  - Gate: stepping matches runtime behavior for a script with loops + calls +
    branches

# Phase 4 — Vertical slice parity (walking + gravity + doors)

- [ ] Physics: stable gravity + floor contact
  - Gate: no “hover until move”, no “fall through after leaving railing”, no
    NaNs
- [ ] Coordinate system: unify Blitz3D ↔ three.js conversions
  - Gate: spawn points and movement directions match expectations (no inverted
    WASD, no axis mixups)
- [ ] Doors: implement buttons + door state + animation
  - Gate: open/close at least one real SCP:CB door in the slice with correct
    collision changes

# Phase 6 (optional) — Standard WASM debug metadata (don’t block bbdbg)

- [ ] Emit `name` section for readability
- [ ] Emit `producers` + `build_id`
- [ ] Add encoder offset capture required for source maps/DWARF
- [ ] Decide on `.wasm.map` vs DWARF line-table scope for BB
